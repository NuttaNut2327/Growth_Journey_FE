import 'package:flutter/material.dart';

class LikedBlogsPage extends StatelessWidget {
 const LikedBlogsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}